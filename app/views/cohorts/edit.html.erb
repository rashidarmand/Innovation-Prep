<h1>Editing <%= @cohort.name %> Cohort</h1>

<%= render 'form', cohort: @cohort %>

<%= link_to 'Show', @cohort, class: "btn btn-primary text-white" %> |
<%= link_to 'Back', cohorts_path, class: "btn btn-primary text-white"%>
